---
product: weconduct
version: 0.9.0
doc_id: weconduct:guide:node-configuration
---

# 节点配置

每个节点的配置参数保存在节点自身的配置中。编辑器会根据节点的类型自动显示对应的配置控件，不同节点的配置界面会有所不同。

## 字段编辑器

节点配置面板会根据参数类型提供不同的编辑控件：

- **文本、数字、布尔值**：直接输入或切换开关。
- **类型化值**：可以区分字符串、数字、布尔、对象等不同类型，确保值的类型正确。
- **对象映射**：用于编辑变量表、输入输出映射等结构化数据。
- **复杂值**（分支列表、代码等）：建议在 **元数据编辑** 面板中完成，编辑空间更大。

节点上的行内摘要只展示最常用的字段，完整配置请切换到 **元数据编辑** 面板查看。

## 敏感数据：避免直接保存明文

> **不推荐**在节点配置中直接保存密码、访问令牌、Cookie、认证头或其他敏感值。节点配置会随项目文件保存，也可能进入备份、复制文件或版本控制记录。

0.9.0 的节点图与元数据编辑器会持续显示这一提示。对于认证或其他运行时秘密，优先使用以下方式：

- 使用 **待输入** 节点，并将对应字段标记为敏感。值只在当前运行会话的内存中保留；会话结束后会销毁。
- 使用 **加密初始参数**。运行项目时输入密码解锁，参数值不会以明文写入项目参数集合。

出于兼容或特殊自动化需求，仍允许在节点配置中直接填写明文值。这是由用户承担风险的入口：请限制项目文件访问权限，不要提交包含秘密的项目文件，也不要把它作为默认认证方案。

## 端口与配置

某些控制节点的配置会影响可用端口。修改配置后建议检查一下已有的连线，确保没有引用到被移除的端口。保存或运行时程序会自动校验端口是否正确。

网络节点保留配置端口与显式数据端口两条入口。对同一参数，执行时只要输入端口存在有效值，就覆盖节点面板中的配置；没有有效输入时才使用节点配置，再向网络上下文和程序默认值回退。网络节点还可以选择 `inherit`、`new`、`anonymous`、`fork`、`switch` 或 `reset` 上下文策略，用于连续会话、匿名请求和相互隔离的分支。

认证、Cookie、TLS、代理和响应限制等敏感或运行期配置建议通过加密参数、待输入字段或受控输入端口传入。节点面板仍允许直接填写明文，保存后会随项目文件持久化，属于不推荐的特殊用法。

## 保存冲突

如果多个窗口或异步刷新同时保存，可能会出现版本冲突。默认情况下程序会保留当前编辑的内容，你也可以在首选项中调整为更严格的冲突检查策略。

## 与 Debug 的边界

Debug 期间对变量值的修改属于临时会话状态，不会自动保存回节点的配置中。如果需要持久化 Debug 中的改动，请在节点配置面板中手动更新。
